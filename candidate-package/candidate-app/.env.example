# Base URL of the JamJoys NestJS backend.
# When running the backend locally, use your machine LAN IP (not localhost)
# so a physical device / emulator can reach it, e.g. http://192.168.1.10:3000
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000
